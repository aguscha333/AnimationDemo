import {StyleSheet} from 'react-native';
import {RED} from '../../../constants/colors';

export default StyleSheet.create({
  box: {
    width: 100,
    height: 100,
    backgroundColor: RED,
  },
});
